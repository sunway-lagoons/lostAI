"""AI inference service (Flask).

Routes:
  POST /embed   Accept one or more image files; return an ordered embedding per image.
  GET  /health  Report service readiness.

No object IDs, tags, database access, or ranking logic.
"""

# TODO: define Flask app with /embed and /health
