"""Gradio screens: upload reference views, submit query images, show results.

Talks to the app backend only (never the inference service directly).
"""

# TODO: build Gradio interface (register object, match query, text search)
