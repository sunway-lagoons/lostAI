"""Persistence for object, tag, image, and embedding records (SQLite).

Image files live on disk; only their paths and metadata are stored here.
"""

# TODO: schema init + CRUD for objects, tags, images, embeddings
